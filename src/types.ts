// Re-export root-level type definitions for compatibility with imports that
// reference `src/...` modules. Keeping a single canonical types file at the
// project root avoids duplication.

export * from '../types';
