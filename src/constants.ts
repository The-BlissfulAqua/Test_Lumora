// Re-export the project's root constants so files under `src/` can import
// using the same relative paths they expect. Keep this file intentionally
// minimal to avoid duplicate definitions.

export * from '../constants';
